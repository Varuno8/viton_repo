datasource db {
  provider = "sqlite"
  url      = env("DATABASE_URL")
}

generator client {
  provider = "prisma-client-js"
}

model User {
  id           String     @id @default(cuid())
  name         String?
  email        String     @unique
  passwordHash String?
  createdAt    DateTime   @default(now())
  tryOnJobs    TryOnJob[]
}

model Product {
  id          String   @id @default(cuid())
  handle      String   @unique
  title       String
  description String?
  brand       String?
  category    String?
  price       Float    // use Float instead of Decimal for SQLite
  colors      String   // store as comma-separated string, parse in app
  sizes       String   // same
  imageUrls   String   // same (pipe/comma separated URLs)
  createdAt   DateTime @default(now())
  tryOnJobs   TryOnJob[]
}

// enums not supported → store status as String
model TryOnJob {
  id             String   @id @default(cuid())
  user           User     @relation(fields: [userId], references: [id])
  userId         String
  product        Product  @relation(fields: [productId], references: [id])
  productId      String
  userPhotoUrls  String   // store as comma-separated string
  status         String   @default("PENDING")
  resultImageUrl String?
  error          String?
  externalJobId  String?
  createdAt      DateTime @default(now())
  updatedAt      DateTime @updatedAt
}
